#ifndef VESTI_FAKE_IO_H_
#define VESTI_FAKE_IO_H_

// the purpose of this header is moking the original io.h header shipped by
// msvc because the original one makes so much errors.

#include <stdio.h>

#endif // VESTI_FAKE_IO_H_
