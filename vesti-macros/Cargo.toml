[package]
name = "vesti-macros"
version = "0.1.0"
edition = "2024"
description = "Compile-time helpers for vesti (string-set matchers)."
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"

